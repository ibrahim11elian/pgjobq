# Generated by `npm run gen:docs` from the transition table in
# packages/core/src/engine/state-machine.ts. Its format is owned by the generator, and CI
# checks it with a git-diff of the regenerated output. If Prettier also reformatted it, the
# two checks would fight: formatting would break the gen-docs diff, and regenerating would
# break format:check.
docs/state-machine.md

# Build output and dependencies.
**/dist/
node_modules/
package-lock.json
